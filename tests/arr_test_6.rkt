;(define (recur [arr : (Vectorof Integer)] 
;			   [low : Integer]
;			   [high : Integer]
;			   [cmp : (Integer Integer -> Boolean)]) : Void
;  (if (eq? (- high low) 1)
;	(Void)
;	(let ([mid (
;	(partt arr low high)
;

;(define (partt [arr : (Vectorof Integer)] 

(let ([x 8])
  (let ([y x])
	(let ([z (+ x y)])
	  (let ([a (+ x (+ y z))])
		(+ (/ a (+ x y))
		   (* (/ x 2) (/ 4 y)))))))

